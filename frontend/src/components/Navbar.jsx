import React from 'react';

const Navbar = () => {
    return (
        <nav className="navbar glass-card">
            <div className="nav-logo">LibFlow</div>
            <div className="nav-links">
                <button className="btn btn-primary">Add Book</button>
            </div>
        </nav>
    );
};

export default Navbar;
